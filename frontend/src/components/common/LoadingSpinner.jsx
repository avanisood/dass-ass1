import React from 'react';

// Loading Spinner component
const LoadingSpinner = () => {
  return (
    <div className="loading-spinner">
      <p>Loading...</p>
      {/* Will be styled with CSS or use Material-UI CircularProgress */}
    </div>
  );
};

export default LoadingSpinner;
